version: "3.9"

services:
  telegram_antilurk_bot:
    image: ghcr.io/your-org/telegram-antilurk-bot:latest
    container_name: telegram_antilurk_bot
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      - DATA_DIR=/data
      - CONFIG_DIR=/data/config
      # Optional NATS integration for events/logs
      # - NATS_URL=nats://nats:4222
      # - NATS_SUBJECT_PREFIX=antilurk
      # Timezone for human-readable timestamps
      - TZ=UTC
    volumes:
      - /volume1/telegram_antilurk_bot/data:/data
    # networks:  # Not required for single-service deployment
    #   - antilurk

# networks:
#   antilurk:
#     driver: bridge

